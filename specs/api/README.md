# API specifications

Đặt ở đây các contract về endpoint, request/response, status code, validation, authentication, authorization, database và compatibility.

Mỗi thay đổi API phải nêu rõ consumer, backward compatibility, ownership/security và test cases. Tài liệu triển khai tương ứng nằm trong docs/api/.
